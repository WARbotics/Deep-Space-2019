package frc.robot.common;

import java.io.File;
import java.io.IOException;
/*
public class Logger{
    String name = ""+".txt"; 
    // I should make it that it sends it to my computer if it see that we are not an tournament 
    public Logger(String name){
        this.name = name; 
    }

    public void createFile(){
        // Creates the file 
    }
    public void testCaseMessage(Boolean status, String message){
        // This is going to the write to the file and has the [x] or [√] to say if the test case worked
        if(status){
            // The message was successful thing 
        }else{

        }
    }
}
*/